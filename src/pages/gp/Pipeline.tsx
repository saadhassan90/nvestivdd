import { GpPagePlaceholder } from "@/components/gp/GpPagePlaceholder";
export default function Pipeline() {
  return (
    <GpPagePlaceholder
      title="Pipeline"
      description="Cross-raise aggregate of all L2+ LP relationships. The join between the CRM and per-raise pipelines."
    />
  );
}